# Local environment (secrets)
.env
.env.test

# Go build artifacts (anchored — an unanchored "packbot" once silently
# excluded cmd/packbot/ from a commit)
/packbot.exe
/packbot

# Frontend lives in its own nested repo
PackSite/

.claude/

# Local Node-era leftovers (untracked, never commit)
node_modules/
cookies.txt
logs/
*.log

# Codex scratch patches and stray build binaries (never commit)
.codex_*.patch
/styledemo.exe
